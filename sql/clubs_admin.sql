-- ================================================================
-- clubs_admin.sql — SQL สำหรับฟีเจอร์ภาพรวมชุมนุม (Admin / ครูวิชาการ)
-- รันใน Supabase SQL Editor ครั้งเดียว
-- ================================================================

-- 1. เพิ่มคอลัมน์ is_academic ในตาราง teachers
ALTER TABLE teachers ADD COLUMN IF NOT EXISTS is_academic boolean DEFAULT false;

-- 2. เพิ่มคอลัมน์ report ในตาราง activities (สำหรับบันทึกรายงานผลการดำเนินงาน)
ALTER TABLE activities ADD COLUMN IF NOT EXISTS report jsonb DEFAULT '{}';

-- ================================================================
-- หลังรัน SQL นี้แล้ว:
-- 1. เข้าระบบ admin → จัดการครู → เลือกครู → ติ๊ก "ครูวิชาการ" → บันทึก
-- 2. ครูวิชาการจะเห็นเมนู "📋 ภาพรวมชุมนุม" ในแถบเมนูซ้าย
-- 3. Admin เห็นการ์ด "ภาพรวมชุมนุม" ใน Admin Panel
-- ================================================================
